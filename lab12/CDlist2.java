
/**
 * Dan McEvoy
 * Robert Derveloy
 * Michael Opalinski
 */
import java.util.ArrayList;
/**
 * this CDlist contains a collection of CDs.
 * 
 */
public class CDlist
{

    private ArrayList<CD> catalog;

    {
        catalog = new ArrayList<CD>();
        String[] tracks = newCD.getSongs();
    }
    /**
     * Adds a new CD to this list
     */
    public void addCD(CD newCD)
    {
        catalog.add(newCD);
    }
    /**
     * Create loops to print out the Artist's name, the ablum name and then the track title. 
     */
    public void PrintCD();
    {
        for (int i = 0; i < catalog.size(); i++)
        {
            CD newcd = catalog.get(i);
            System.out.println(newCD.getArtistName());
            System.out.println(newCD.getAlbumName());
            newCD.getSongs();
        }
    public void TrackList()
    {
        for ( int = 0; i < newCD.getSongs(); i++)
        {
            System.out.println(newCD.getSongs();
}
    
    
        
{
  
    
}
}